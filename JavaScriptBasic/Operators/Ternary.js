age=17
let result = (age>=18) ? "Adult" : "Minnor"
console.log(result)

//Nested Tarnary
let marks = 85;
let grade = marks >= 90 ? "A"
           : marks >= 75 ? "B"
           : marks >= 60 ? "C"
           : "F";

console.log(grade); 
